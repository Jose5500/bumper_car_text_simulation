package com.example.company;


public class Main {

    public static void main(String[] args) {
        Grid grid = new Grid();
        GameController controller = new GameController(grid);
    }
    //create the input methods and then make it so the user can input the location and the direction where the bumper cars will face
}
